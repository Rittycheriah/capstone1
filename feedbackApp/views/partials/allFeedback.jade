.col-md-9
  .col-md-10(ng-controller='showAllActivePropFeedback')
    h1 {{title}}
    h3 Welcome {{rootuser}}
    .list.homeText(ng-repeat= 'unit in units')
      .info.property Property: {{unit.HouseNum}}  {{unit.StreetName}} {{unit.City}}, {{unit.State}}  {{unit.Zip}}
      .info.totalCalls Total Number of Calls : {{unit.callCount}}
      .list.actions
        button.btn.btn-default.actionBtns 
          a.actionLink1(ng-click='addNewCall(unit._id)') Add Call Only  
        button.btn.btn-info.actionBtns
          a.actionLink2(ng-click='feedbackAdd(unit._id)') Add Showing
        button.btn.btn-default.actionBtns
          a.actionLink1(ng-click='postShow(unit._id)') Add Post Showing Feedback
        button.btn.btn-info.actionBtns  
          a.actionLink2(ng-click='changeDetsStatus(unit._id)' ng-hide='showDets == unit._id') Showing Data
      br
      br
      .details(ng-show='showDets == unit._id')
        .info.totalShowings Total Number of Showings : {{details.length}}
        table#customTable.table.table-bordered.table-hover
          tr
            td Contact Name
            td Phone Number
            td Status
            td Follow Up Needed
            td Client Response
          tr(ng-repeat= 'detail in details', style='border: 1px solid black; border-collapse: separate; border-spacing: 2px; padding: 20px;')
            td {{detail.name}}
            td {{detail.phone}}
            td {{detail.status}}
            td {{detail.responseAfterShowing | needsFollowup: detail.responseAfterShowing }}
            td {{detail.responseAfterShowing}}
        a(ng-click='hideDetsAgain()') Collapse